# --- BACKEND VARIABLES ---

# Database (Supabase PostgreSQL Connection String)
# Find in Supabase -> Settings -> Database -> Connection String -> URI
DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres"

# Supabase Services (Storage & Vector)
# Find in Supabase -> Settings -> API
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_KEY="your-anon-key"

# AI Model
GOOGLE_API_KEY="your-gemini-api-key"

# Security
# Run `openssl rand -hex 32` to generate
SECRET_KEY="generate-a-random-secret-string"

# CORS (Frontend domains allowed to access backend)
# Comma-separated list. Add your Vercel URL here after deployment.
BACKEND_CORS_ORIGINS='["http://localhost:5173", "https://your-production-app.vercel.app"]'

# --- FRONTEND VARIABLES (Set these in Vercel) ---

# Backend URL
VITE_API_URL="https://your-backend-app.onrender.com"

# Optional: Payment
VITE_UPI_ID="your-id@upi"
VITE_ADMIN_EMAIL="admin@gmail.com"
